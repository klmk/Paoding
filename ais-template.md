# AI 实施规格书（AIS）模板

> **定位：** AIS（AI Implementation Specification）是 PRD 到代码之间的翻译层。PRD 是给人看的（自然语言、允许模糊），AIS 是给 AI 看的（结构化、不允许模糊）。
> **生成方式：** AI 基于 PRD 自动生成，产品经理审核确认。
> **使用方式：** Phase 2/3 中 AI 基于 AIS（而非 PRD）进行架构设计和编码实现。

---

## 1. 数据契约（I/O Contract）

### 1.1 接口/函数输入定义

对每个需要实现的接口或函数，定义精确的输入：

| 字段名 | 类型 | 必填 | 格式/约束 | 示例值 | 说明 |
|--------|------|------|----------|--------|------|
| | | | | | |

### 1.2 接口/函数输出定义

**成功响应：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| | | |

**失败响应：**

| 错误码 | 含义 | 触发条件 |
|--------|------|---------|
| | | |

### 1.3 副作用

| 操作 | 描述 |
|------|------|
| | |

---

## 2. 业务规则表（Decision Table）

| 条件 | 动作 | 备注 |
|------|------|------|
| | | |

---

## 3. 状态机（State Machine）

```
状态A → (触发条件) → 状态B
状态B → (触发条件) → 状态C
...
```

| 当前状态 | 触发条件 | 目标状态 | 动作 |
|---------|---------|---------|------|
| | | | |

---

## 4. 异常处理矩阵

| 异常类型 | 触发条件 | 处理方式 | 用户提示 | 重试策略 |
|---------|---------|---------|---------|---------|
| | | | | |

---

## 5. 具体示例（Concrete Examples）

### 示例 1：正常流程
- **输入：**
- **期望输出：**

### 示例 2：异常场景
- **输入：**
- **期望输出：**

### 示例 3：边界条件
- **输入：**
- **期望输出：**

---

## 6. 环境与约束清单

| 约束类型 | 具体内容 |
|---------|---------|
| 服务器地址 | |
| API 端点 | |
| 认证方式 | |
| 网络策略 | |
| 超时限制 | |
| 并发限制 | |
| 数据格式要求 | |
| 其他约束 | |

---

## 7. 依赖关系图

```
模块A → 调用 → 模块B（传递数据：xxx）
模块B → 调用 → 模块C（传递数据：xxx）
```

---

## 8. 与 PRD 的映射关系

| AIS 章节 | 对应 PRD 需求编号 | 对应 AC 编号 |
|---------|-----------------|-------------|
| | | |

---

## 使用说明

1. **生成时机：** Phase 1（PRD 确认）完成后，进入 Phase 2 之前
2. **生成方式：** AI 基于 PRD 自动生成 AIS，产品经理逐项审核
3. **审核重点：** 检查 AI 对 PRD 的理解是否正确——AIS 中写的就是 AI 的理解
4. **确认后：** AIS 作为 Phase 2/3 的直接输入，替代 PRD 作为开发的精确规格
5. **变更管理：** PRD 变更时必须同步更新 AIS，AIS 变更时必须重新确认
